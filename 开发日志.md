# 开发日志

## 2026-05-10

- 读取需求文档《服装电商智能选品系统\_需求文档\_v1.0》并提取可复用设计要点：
  - 选品链路需包含：需求输入 → 知识图谱检索/结构化输出 → 店铺数据对比（相似度）→ 分支决策（选/不选）→ 经验库沉淀与唤醒
  - 经验库条目需包含：触发条件、··  根因、重新激活条件（revisit\_conditions）
- 品类管理智能体后续开发方向确定：
  - 保持“规则算 KPI/阈值触发”作为硬逻辑，确保可复现与可 benchmark
  - 引入“建议品类 vs 店铺现有品类”的相似度对比与分支决策输出（对齐需求文档 Step 3）
  - 对“不选品/延后上新”生成结构化负样本（experience\_candidates），为后续经验库落地做准备
- 已在品类管理智能体落地“分支决策 + 负样本候选”最小实现：
  - evaluate\_new 候选项新增：match（是否店铺已有相似品类）、decision（keep\_selling/adjust\_or\_retire/defer\_new/approve\_new）
  - 当触发 defer\_new 或 adjust\_or\_retire 时，生成 experience\_candidates（结构化负样本候选，含 revisit\_conditions）
  - 报告渲染增加：展示候选决策与匹配类型，并显示负样本候选条数
- 已增强 LLM 配置兼容性（对齐 OpenAI SDK 的用法习惯）：
  - 支持从环境变量注入额外请求头 LLM_EXTRA_HEADERS_JSON（用于 X-Failover-Enabled 等）
  - 支持从环境变量注入额外请求体字段 LLM_EXTRA_BODY_JSON（用于 top_k 等）
  - 在 LLM 请求中统一带 max_tokens（LLM_MAX_OUTPUT_TOKENS，最大钳制 4096）
- 修复 LLM 状态展示：
  - 在仅启用品类管理 LLM（llm_scope=category）时，报告会显示 LLM 补充为开启，并额外显示 LLM 范围

## 2026-05-11

- 落地“品类口径做实”的映射接入（SKU→品类）：
  - 增加 category\_mapping 加载器（支持 .json/.csv），并由编排器注入品类管理智能体
  - CLI 新增参数 --category-mapping，将映射路径透传给编排器
- 增强可验证性：品类管理输出增加“类目来源统计”
  - 统计 mapping/keyword/unknown 三类来源的 SKU 数与命中率（mapping\_hit\_rate\_pct）
  - Markdown 报告在品类管理章节展示“类目映射命中 x/y (%)”（仅在提供映射时展示）
- 补齐看板演示链路：
  - FastAPI 接口 /api/report 与 /api/report/markdown 支持 category\_mapping 查询参数
  - Web 看板参数面板新增 category-mapping 输入框，并在“报告上下文”展示类目映射加载失败提示
- 增强课堂演示的“可控 SKU 集”：
  - MockERPData 新增 sku_scenario=class_demo：固定 8 个 SKU，覆盖 畅销/高退货/低库存/高库存/滞销 等典型场景
  - CLI/API/Web 均支持切换 sku_scenario（默认不影响原始随机数据）
- 推进经验库落地（品类维度）：
  - memory 文件结构扩展：新增 category_experiences（兼容原 items 避雷点结构）
  - 编排器支持将品类管理产出的负样本经验落盘，并在下次运行回读，影响 audit_existing/retire 决策卡（经验约束）
- 经验约束“变硬”：经验命中会自动生成可执行动作与检查清单
  - 基于 root_cause 的规则映射生成 guardrail_actions / guardrail_checks，并挂到 experience_guardrail 中
  - Markdown 报告在决策卡中展示关键的经验动作/检查项，方便调试与落地
- 经验动作贯穿到系统行动层：
  - 编排器会把 guardrail_actions/guardrail_checks 汇总进全局 actions（带 owner 分组前缀），用于统一输出与后续联动其它智能体
- LLM 第三层（受约束策略搜索）开始落地：
  - 规则先生成 hard_constraints（预算范围、库存压力阈值、退货率上限、禁止补货品类）
  - 品类管理 LLM 输出结构化三方案（A 保守去化 / B 结构优化 / C 延后上新观察），每个方案含预期影响、风险、7天/30天监控指标
  - 增加策略方案归一化与兜底逻辑，保证在 LLM 输出不完整时仍能产出完整三方案
