# 电商运营 Agent Baseline Demo 使用文档

## 1. 环境要求

- Python 3.9+
- 无需额外依赖（当前 baseline 仅使用标准库）

## 2. 文件说明

本次交付核心内容：

- `ecommerce_agent/`：Demo 源码
- `demo_report.md`：示例运行产出的报告
- `演示使用文档.md`：本说明文档

## 3. 一键运行命令

在项目根目录（`数据科学`）执行：

```bash
python -m ecommerce_agent.main --demo --seed 42 --top-n 5 --as-of 2026-04-06 --output "./output/demo_report.md"
```

## 4. 运行结果

执行后会得到两类输出：

1. 终端打印结构化结果（DEMO CONTEXT / SALES REVIEW / INVENTORY WARNINGS / ACTIONS）
2. 生成报告文件：`demo_report.md`

## 5. 测试命令

```bash
python ecommerce_agent/tests/test_erp.py
python ecommerce_agent/tests/test_trends.py
python ecommerce_agent/tests/test_demo_smoke.py
```

## 6. 可调参数

- `--seed`：固定随机种子，保证结果可复现
- `--top-n`：榜单展示数量
- `--as-of`：报告日期
- `--output`：报告输出路径
- `--replenishment-cycle`：补货周期（天）
- `--overstock-days`：高库存阈值（天）

## 7. 常见问题

1. **终端中文乱码**
  - 不影响结果，`demo_report.md` 为 UTF-8 正常可读。
2. **找不到模块 ecommerce_agent**
  - 请确认命令在项目根目录执行（即包含 `ecommerce_agent/` 的目录）。

